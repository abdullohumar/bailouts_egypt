import './bootstrap';

// Alpine.js is bundled and started by Livewire 4 automatically.
// Do NOT import or start it here — doing so creates two Alpine instances
// which breaks wire:submit, wire:model, and all Livewire directives.
